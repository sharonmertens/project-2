<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <% include ../partials/head.ejs %>
  </head>
  <body>
    <h1>Closet Items Show Page</h1>
    Description: <%=item.description%> <br>
    <img src="<%=item.img%>"> <br>
    Brand: <%=item.brand%> <br>
    Price: <%=item.price%> <br>
    Size: <%=item.size%> <br>
    <% if (item.available === true) { %>
      You are in luck! This item is available!
    <% }  else { %>
      Sorry. This item is unavailable. Check out something else!
    <% } %>
    <!-- ADD FORM FOR DELETE BUTTON  -->
    <form action="/closetswap/<%=item.id;%>?_method=DELETE" method="POST">
      <input type="submit" value="DELETE">
    <!-- LINK BACK TO INDEX PAGE -->
    <nav>
      <a href="/closetswap">Back to Closet Items Page</a>
    </nav>
  </body>
</html>
